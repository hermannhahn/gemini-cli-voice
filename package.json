{
	"name": "gemini-cli-voice",
	"version": "1.4.0",
	"description": "High-quality local text-to-speech (TTS) for Gemini CLI using Piper.",
	"keywords": [
		"TTS",
		"Gemini",
		"CLI"
	],
	"homepage": "https://github.com/hermannhahn/gemini-cli-voice#readme",
	"bugs": {
		"url": "https://github.com/hermannhahn/gemini-cli-voice/issues"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/hermannhahn/gemini-cli-voice.git"
	},
	"license": "ISC",
	"author": "Hermann Hahn",
	"type": "commonjs",
	"main": "dist/tool_code.js",
	"files": [
		"dist",
		"bin",
		"models",
		"gemini-extension.json",
		"GEMINI.md"
	],
	"scripts": {
		"lint": "eslint \"src/**/*.ts\" --fix",
		"test": "echo \"No tests specified\" && exit 0",
		"webpack": "webpack",
		"build": "webpack",
		"cleanup-releases": "node scripts/cleanup-releases.js",
		"update:version": "npm run lint && npm version patch --no-git-tag-version && node scripts/update-version.js && webpack && npm run push:version",
		"push:version": "git add . && node -e \"const v = require('./package.json').version; require('child_process').execSync('git commit -m \\\"chore: bump version to ' + v + '\\\"', {stdio: 'inherit'})\" && git push origin development",
		"push:release": "node -e \"const v = require('./package.json').version; require('child_process').execSync('gh release create v' + v + ' --generate-notes', {stdio: 'inherit'})\"",
		"deploy": "git checkout main && git pull origin main && npm run lint && webpack && git add dist/tool_code.js && git commit -m 'chore: build tool_code.js for release' || true && git push origin main && node -e \"const v = require('./package.json').version; require('child_process').execSync('git tag -f v' + v + ' && git push origin -f v' + v, {stdio: 'inherit'})\" && npm run push:release"
	},
	"dependencies": {
		"@modelcontextprotocol/sdk": "^1.28.0"
	},
	"devDependencies": {
		"@eslint/js": "^10.0.1",
		"@types/node": "^20.19.37",
		"eslint": "^10.0.3",
		"ts-loader": "^9.5.4",
		"typescript": "^5.9.3",
		"typescript-eslint": "^8.57.1",
		"webpack": "^5.105.2",
		"webpack-cli": "^6.0.1",
		"rimraf": "^6.0.1"
	}
}
