description = "Disable automatic voice response mode"
prompt = """
The user requested to disable voice mode via the `/voice:disable` command.

Call the `voice_toggle` tool with `enabled = false`.

After success:
- Confirm to the user that voice mode has been deactivated.
- Go back to responding with text only, unless the `speech` tool is explicitly requested.
"""
