description = "Enable automatic voice response mode"
prompt = """
The user requested to enable voice mode via the `/voice:enable` command.

Call the `voice_toggle` tool with `enabled = true`.

After success:
- Confirm to the user that voice mode is active.
- CRITICAL: From this moment on, you MUST NEVER respond with plain text. Every single word of your response MUST be passed to the `speech` tool.
"""
